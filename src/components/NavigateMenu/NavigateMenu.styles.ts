import { createAppTheme } from '../../theme/theme';
const theme = createAppTheme();

export const classes = {
  navigateMenu: {
    display: 'flex',
    alignItems: 'center',
  },
  menuButton: {},
};
